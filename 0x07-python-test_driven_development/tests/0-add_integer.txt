This library contains one function called ``add_integer``

add_integer function checks if a and b are floats. If they are,
it casts them into integers
Import the function
   add_integer = __import__('0-add_integer').add_integer

Add the 2 floats
    add_integer(1.3, 2.4)
   outputs 3

Add the float to int
    add_integer(1.3, 2)
    outputs 3

add_integer function checks if the numbers are integers, if they
are not it raises TypeError. If the numbers are integers,
it adds them together to return an integer.
try adding 2 strings
    add_integer('h', 'e')
    Traceback (most recent call last):
    outputs a TypeError of message:  a must be an integer

try adding 2 integers
    add_integer(4, 5)
    outputs 9

try adding a string to int
    add_integer('h', 1)
    Traceback (most recent call last):
   outputs a  TypeError of message: a must be an integer

Adding nothing/nada
    add_integer(None)
    Traceback (most recent call last):
   outputs a TypeErroror of message: a must be an integer.
